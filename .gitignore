__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ruff_cache/
.uv-cache/
.env
*_pb2.py
# Runtime modules import these bindings directly. Keep the package's generated
# bindings versioned so clean installs work and --check can detect stale output.
!src/dwarf_alpaca/proto/*_pb2.py
var/
venv/
.pytest_cache/
.test-*/
.coverage
dwarf_python_api/
build/
dist/
DwarfAlpacaGUI.spec
logs/
dwarfii_api/
apk/*.apk
apk/*.apks
.vscode/settings.json
scripts/Updater/*

# Disposable firmware extraction and decompiler output. Keep only curated
# metadata, scripts, and documentation under version control.
firmware-analysis/extracted/
firmware-analysis/work/
firmware-analysis/ghidra-projects/

# Original vendor firmware may contain embedded network credentials and is
# retained locally only. Commit hashes, inventories, and redacted metadata.
firmware/*.zip

# Keep model-specific DRACO reverse-engineering notes local until they are
# intentionally reviewed and published.
docs/apk-analysis/DRACO_LOCAL.md

# Local astronomical captures and data
captures/
log/
*.fits

# Local test plans, backups and scratch scripts
dwarf_schedules_backup.json
plan_*.json
sched*.json
sequence.json
scratch_*.py
*.code-workspace

